using UnityEngine;

public class ohkawa_fallScript : MonoBehaviour
{
    int jumpPower = JumpPowerScript.JumpPower;  //ジャンプするシーンからジャンプのパワーを引き継ぎ。

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}